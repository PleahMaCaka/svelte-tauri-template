{
  "name": "svelte-tauri-template",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@biomejs/biome": "^2.3.6",
    "lefthook": "^2.0.4"
  },
  "scripts": {
    "check": "biome check",
    "format": "biome check --write",
    "build": "bun run --filter \"*\" build"
  }
}
