{
  "name": "svelte-tauri-template",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@biomejs/biome": "^2.1.3",
    "lefthook": "^1.12.2"
  },
  "scripts": {
    "check": "biome check",
    "format": "biome check --write",
    "build": "bun run --filter \"*\" build"
  }
}
