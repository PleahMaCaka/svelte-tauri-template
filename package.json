{
  "name": "svelte-tauri-template",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@biomejs/biome": "^2.2.4",
    "lefthook": "^1.13.4"
  },
  "scripts": {
    "check": "biome check",
    "format": "biome check --write",
    "build": "bun run --filter \"*\" build"
  }
}
