{
  "name": "svelte-tauri-template",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@biomejs/biome": "^2.2.0",
    "lefthook": "^1.12.3"
  },
  "scripts": {
    "check": "biome check",
    "format": "biome check --write",
    "build": "bun run --filter \"*\" build"
  }
}
