{
  "name": "svelte-tauri-template",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@biomejs/biome": "^2.5.3",
    "lefthook": "^2.1.10"
  },
  "scripts": {
    "check": "biome check",
    "format": "biome check --write",
    "build": "bun run --filter \"*\" build"
  }
}
